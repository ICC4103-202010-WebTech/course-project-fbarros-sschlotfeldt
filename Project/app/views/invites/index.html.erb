<div class="row">
  <div class="col-3 bg-dark"></div>
  <div class="col-lg-6">
    <table class="col-lg-12 bg-light">

        <thead>
          <tr>
            <th>UserName</th>
            <th>Invite</th>
          </tr>
        </thead>
        <%@user_for_invite.each do |y|%>
        <%@event_members.each do |x|%>
        <tbody class="border">
          <tr>
            <%if y.id == x.user_id %>
            <%else%>
              <td><%=@user_for_invite.find(y.id).userName %></td>
            <%end%>
          </tr>
        </tbody>

        <tfoot>
        </tfoot>
        <%end %>
        <%end %>
    </table>
  </div>
  <div class="col-3 bg-dark"></div>
</div>

