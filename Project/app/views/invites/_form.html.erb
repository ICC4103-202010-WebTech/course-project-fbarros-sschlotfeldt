
<div class="row ml-0">
  <div class ="form-group">
    <%= form_with model: @invite, local: true do |v| %>
      <div class="form-group">
        <%= v.label :event %>
        <%= v.collection_select(:event, Event.where(user_id: $current_user[0].id).order(title: :asc), :id, :title, {}, { class: "form-control" })%>
      </div>
      <div class="form-group">
        <%= v.label :users %>
        <%= v.collection_select(:users, User.all.order(userName: :asc), :id, :userName, {}, { class: "form-control" }) %>
      </div>
      <%= v.submit :Submit, class: ["btn", "btn-primary"] %>
    <%end%>
  </div>
</div>
