<%= form_with(model: o_invite, local: true) do |form| %>
  <% if o_invite.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(o_invite.errors.count, "error") %> prohibited this o_invite from being saved:</h2>

      <ul>
        <% o_invite.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
