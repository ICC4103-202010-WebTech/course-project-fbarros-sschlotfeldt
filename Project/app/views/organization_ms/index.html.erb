<p id="notice"><%= notice %></p>

<h1>Organization Ms</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @organization_ms.each do |organization_m| %>
      <tr>
        <td><%= link_to 'Show', organization_m %></td>
        <td><%= link_to 'Edit', edit_organization_m_path(organization_m) %></td>
        <td><%= link_to 'Destroy', organization_m, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Organization M', new_organization_m_path %>
